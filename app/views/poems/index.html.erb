<div class="container">
  <div class="wrapper col-md-8 col-md-offset-2 col-sm-10">
      <h1>Achieve Poems</h1>
      <div class="row">
          <% @poems.each do |poem| %>
          <div class="row">
            <p><%= poem.title %></p>
            <p><%= poem.content %></p>
            <p><%= link_to "詳細表示", poem_path(poem.id) %></p>
        　</div>
    <% end %>
  </div>
</div>